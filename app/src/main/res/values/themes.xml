<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
        El fondo de ventana es lo que asoma tras las barras del sistema y
        durante el arranque, antes de que Compose dibuje. Con el tema
        Material.Light por defecto salía blanco.
    -->
    <style name="Theme.Practicapro" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/fondo</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:enforceStatusBarContrast">false</item>
        <item name="android:enforceNavigationBarContrast">false</item>
    </style>
</resources>
